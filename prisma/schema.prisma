// This is the Prisma schema file.
// Models are added in T-003 (database schema task).
// Do not add models here — wait for T-003.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
